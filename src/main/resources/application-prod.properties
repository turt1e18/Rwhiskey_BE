# Production (Railway) Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Security Prod (Strict)
app.security.csrf.enabled=true
app.cors.allowed-origins=https://turt1e18.work,https://www.turt1e18.work
server.servlet.session.cookie.secure=true
server.forwarded-headers-strategy=framework
server.servlet.session.cookie.domain=turt1e18.work
server.servlet.session.cookie.same-site=none